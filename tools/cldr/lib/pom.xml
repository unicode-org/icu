<?xml version="1.0" encoding="UTF-8"?>
<!-- © 2020 and later: Unicode, Inc. and others.
     License & terms of use: http://www.unicode.org/copyright.html
     See README.txt for instructions on updating the local repository.
     -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- This POM file acts as a parent POM file for any tool which is built
         via Maven and requires access to the CLDR data APIs. This POM file
         and the other files in this directory encapsulate the somewhat messy
         task of including the Ant-built CLDR JAR file in Maven projects. -->

    <!-- Declares this to be a POM that's included by other POM files. -->
    <packaging>pom</packaging>    
    
    <!-- This must match any child POM file's <parent> declaration. -->         
    <groupId>org.unicode.icu</groupId>
    <artifactId>cldr-lib</artifactId>
    <version>1.0</version>

    <repositories>
        <!--
            See http://cldr.unicode.org/development/maven#TOC-Configuring-Maven 
            for configuration and access control.
            
            And generally,
            https://docs.github.com/en/packages/using-github-packages-with-your-projects-ecosystem/configuring-apache-maven-for-use-with-github-packages
        -->
        <repository>
            <id>githubcldr</id>
            <name>Maven@unicode-org/cldr</name>
            <url>https://maven.pkg.github.com/unicode-org/cldr</url>
        </repository>
    </repositories>
    
    <!-- See https://github.com/unicode-org/cldr/packages/521051/versions for possible versions 
        you can get the XML directly from that page.
        -->
    <dependencies>
        <dependency>
        <groupId>org.unicode.cldr</groupId>
        <artifactId>cldr-code</artifactId>
        <version>0.0.0-SNAPSHOT-feb602b06b</version>
        </dependency>
    </dependencies>
</project>

